<!--<view>
  <image class="img" src="{{src}}" mode="widthFix"></image>
</view>-->

<!--<view>
  <block wx:if="{{previous>0}}">
    <button class="buttonleft" bindtap="showimg" data-id="{{mhid}}" data-st="{{previous}}">上一章</button>
  </block>
  <block wx:else>
    <button class="buttonleft disabled">上一章</button>
  </block>

  <block wx:if="{{next>0}}">
    <button class="buttonright" bindtap="showimg" data-id="{{mhid}}" data-st="{{next}}">下一章</button>
  </block>
  <block wx:else>
    <button class="buttonleft disabled">下一章</button>
  </block>
</view>-->

<scroll-view scroll-y style="height:{{screenHeight}}px;" bindscrolltoupper="upper" bindscrolltolower="lower" upper-threshold="50" lower-threshold="1">
  <block wx:for="{{list}}" wx:for-item="item" wx:key="item.f_sort">
    <image class="img" src="{{item.src}}" mode="widthFix"></image>
  </block>
</scroll-view>